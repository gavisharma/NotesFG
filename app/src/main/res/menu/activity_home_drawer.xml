<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/sort_by_subject"
            android:icon="@drawable/ic_sort_subject"
            android:title="Subject" />
        <item
            android:id="@+id/sort_by_date"
            android:icon="@drawable/ic_sort_date"
            android:title="Date" />
        <item
            android:id="@+id/sort_by_title"
            android:icon="@drawable/ic_sort_title"
            android:title="Title" />

    </group>

    <item android:title="Subject">
        <menu>
            <item
                android:id="@+id/add_subject"
                android:icon="@drawable/ic_add_subject"
                android:title="Add" />
            <item
                android:id="@+id/remove_subject"
                android:icon="@drawable/ic_delete_subject"
                android:title="Remove" />

            <item
                android:id="@+id/show_subject"
                android:icon="@drawable/ic_subject"
                android:title="Show" />
        </menu>
    </item>

</menu>
